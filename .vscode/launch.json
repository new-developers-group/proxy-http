{
  "configurations": [
    {
      "type": "aws-sam",
      "request": "direct-invoke",
      "name": "proxy-http:ProxyFunction",
      "invokeTarget": {
        "target": "template",
        "templatePath": "${workspaceFolder}/template.yaml",
        "logicalId": "ProxyFunction"
      },
      "lambda": {
        "payload": {},
        "environmentVariables": {}
      }
    }
  ]
}
